// Build folder is derived from the current git branch:
//   main     → dist/sitrec
//   weather  → dist/weather
// Override buildFolder below if you want a fixed output location.

const { execSync } = require('child_process');
const path = require('path');

let branch;
try {
    branch = execSync('git rev-parse --abbrev-ref HEAD', { encoding: 'utf-8' }).trim();
} catch {
    branch = 'main';
}

const buildFolder = (branch === 'main' || branch === 'HEAD') ? 'sitrec' : branch;

module.exports = {
    dev_path: path.resolve(__dirname, '..', 'dist', buildFolder),
    prod_path: path.resolve(__dirname, '..', 'dist', buildFolder),
    buildFolder: buildFolder,
}
